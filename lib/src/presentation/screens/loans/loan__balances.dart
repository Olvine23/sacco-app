import 'package:flutter/material.dart';

class LoanBalancesScreen extends StatelessWidget {
  const LoanBalancesScreen({super.key});

  @override
  Widget build(BuildContext context) {

    
    return  Scaffold(
      appBar: AppBar(title: Text("My Loans")),
      body: Container(),
    );
  }
}