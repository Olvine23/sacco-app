import 'package:flutter/material.dart';

class LoanGuaranteed extends StatelessWidget {
  const LoanGuaranteed({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Loans Guaranteed"),),
    );
  }
}